# SYNNR Truth Audit — marketing claims vs. code

**2026-08-21, pre-selling.** Every factual claim on the marketing surfaces
(homepage, /products/*, /demo, /onepager, /readiness-audit, footer, metadata)
traced to the code that implements it. Worst first. Verification method noted
where it wasn't just reading code: `[live]` = exercised against production,
`[test]` = pinned by the vitest suite, `[sql]` = checked in the production DB.

## Verdict table — worst first

| # | Claim (where) | Verdict | The truth in code |
|---|---|---|---|
| 1 | **"You get the text before it lapses"** (homepage, How-it-works №03) | **FALSE — lie-grade** | Alerts are **email only**. The sweep guards every SMS attempt behind `smsConfigured()` ([lib/saas/notify.ts](lib/saas/notify.ts)) and Twilio has no credentials — texts have never sent. The in-app settings page is honest about this ("Text alerts activate once SMS credentials are connected — email works now"); the homepage is not. One-word fix staged: "text" → "email." |
| 2 | "Cancel anytime" (pricing fineprint, hero) | **UNVERIFIABLE** | The Stripe **customer portal is wired** ([app/api/saas/portal/route.ts](app/api/saas/portal/route.ts)) — but whether *cancellation* is enabled inside it is a Stripe **dashboard setting** code can't see. 30-second check: Stripe → Settings → Customer portal → confirm "cancel subscriptions" is on. Until then the claim rests on a toggle nobody has looked at. |
| 3 | Stripe billing end-to-end (per-yard quantity, checkout → active) | **PARTIAL** | Fully coded and heavily tested [test: 134 passing incl. cap/allowance/reconcile] — but **no real purchase has ever exercised the webhook**, and the webhook URL (www vs apex) is unconfirmed in the Stripe dashboard. The Spy Coil Tubing $500 self-purchase remains the one test that settles it. Sellable, but do the purchase before the first customer does. |
| 4 | "Setup is free for the first 10 yards — I'll do it with you in one afternoon" (pricing, onepager, demo) | **TRUE-as-pledge** | Not code — a personal commitment. True the day you honor it, a lie the day you don't. Same class: "I'll call or text you this week" on the readiness-map form. |
| 5 | Demo "shreds itself after 24 hours" (/demo) | **TRUE (first cycle today)** | Reaper deployed in the watchdog cron ([lib/saas/demo-seed.ts](lib/saas/demo-seed.ts) `cleanupDemoCompanies`); deployed *after* yesterday's 8:30am run, so its **first live reap is today 8:30am CT**. Code path exists; confirm the run's `demo reaped xN` detail today. |
| 6 | "the heads-up lands the night before it would've bitten you" (product narrative) | **PARTIAL — wording** | Better than claimed, not as phrased: the daily 6:30am sweep warns with a configurable **lead window (default 30 days)** — you're warned *weeks* before, not the night before. Nobody could call this a lie (you get MORE warning), but tighten to "days before" for literalism. |
| 7 | **Automated expiration alerts** (everywhere) | **TRUE** | Real scheduled job (Vercel cron 11:30 UTC → [app/api/cron/saas-alerts](app/api/cron/saas-alerts/route.ts)), real **email delivery via Resend** `[live: sends delivered Aug 19]`, per-recipient yard routing, once-per-item dedup with renewal re-arm [test], failed-send retry, and a three-layer dead-man's switch (sweep heartbeat → watchdog email → external GitHub pinger) that has **caught a real failure and self-healed** `[live]`. This is the strong version of the claim, not the login-warning version. |
| 8 | Proof links: shareable, read-only, live, revocable (/products/proof, /demo) | **TRUE** | Public tokened route, 144-bit unguessable token `[sql: gen_random_bytes(18)]`, read-only by construction (no mutations on the page), **live-computed on every load**, revocable from Settings → Proofs (`revoked_at`), scope-checked so a link leaks nothing outside its company/yard/unit `[live: bad token → clean invalid page]`, noindexed. |
| 9 | "Your data, exportable" (pricing, terms) | **TRUE** | One-click CSV: [app/api/saas/export/route.ts](app/api/saas/export/route.ts) — every compliance item with yard/unit/asset context, RLS-scoped, download button in Settings. Format: CSV. |
| 10 | Multi-yard, one account, data separated | **TRUE** | Yards table, per-yard scoping on board/alerts/billing, company switcher, per-yard Stripe quantity with hard cap [test]. Demo seeds one yard; the app supports many. |
| 11 | Roles (owner/admin/member) | **TRUE** | Three roles enforced **server-side and in RLS row policies** `[sql: policy map verified live]` [test: role matrix pinned]. Invites + self-signup passwords + ownership transfer work `[live]`. Marketing's "dispatchers, safety managers, foremen" are personas ("for" statements), not role claims — fine. |
| 12 | **"Readiness checks with no override"** (homepage, products/roll) | **TRUE — literally** | The verdict is computed server-side and the record action writes `status: comp.verdict` — *"computed, never chosen"* ([dispatch/actions.ts](app/app/units/[unitId]/dispatch/actions.ts)). No live code path can record a not-ready truck as ready. (`not_ready_override` appears only as a *display* label for legacy rows from before the override was removed.) |
| 13 | Mobile: usable on a phone in portrait | **TRUE** | Purpose-built mobile layouts (verdict banner, quick action, bottom tabs), glove-sized targets, and a programmatic overflow audit across site + app at 375px `[live: zero overflowing elements; proof-page table fixed during the audit]`. |
| 14 | **Demo drift** | **TRUE — drift-proof by construction** | /demo doesn't imitate the app; it **seeds a private company and signs the visitor into the real app**. What a buyer sees in the demo *is* what he receives. The only static assets (two hero photos) are photographed from the same fixture cast (Caprock / CT-03) as the seeded yard `[live: proof link cross-references the board line-for-line]`. |
| 15 | Demo specifics: "20 units, 45 hands," private copy, "nothing you click touches anyone else's" | **TRUE** | `[sql: 21 units (20 rolling + shop), 45 crew, 223 items per copy]`; isolation proven with a live mutation visible in exactly one visitor's copy `[live]`. |
| 16 | Import: "in by lunch," preview-first, no duplicates (products/cards) | **TRUE** | CSV importer with dry-run preview, idempotent re-import, header-alias dictionary, impossible-date rejection [test]. |
| 17 | Camera renew + OCR (products/roll: "shoot the new cert… confirm the date it reads") | **TRUE** | Photo → OCR pre-fill → **save blocked until human confirms** the read date [code + copy match exactly]. |
| 18 | The math: "$10k+ NPT day," "one prevented day covers more than a year and a half," "$500 flat, never per-seat, no contract" | **TRUE** | $10,000/$500 = 20 months ✓; illustration disclaimer present on the cost table; billing is quantity-per-yard monthly with no seat dimension and no term. |
| 19 | "No fake logos, no made-up numbers. We're onboarding our first shops now" (founder section) | **TRUE** | The site contains zero logos, zero customer counts, zero testimonials — and says so out loud. |

## What I'd need from you to close the UNVERIFIABLEs
1. **Stripe dashboard, 30 seconds:** customer portal → cancellation enabled? And webhook endpoint URL starts with `https://www.` (not apex).
2. **The Spy Coil Tubing $500 self-purchase** — the only end-to-end money-path test that exists. Do it before a customer does it first.
3. Today ~8:35am CT: I confirm the demo reaper's first cycle actually reaped.
