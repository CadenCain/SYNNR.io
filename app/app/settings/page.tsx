import PhaseStub from "../_components/phase-stub";

export default function SettingsStub() {
  return (
    <PhaseStub
      title="Settings"
      phase="Coming in Phases 6–8"
      blurb="Team & roles, billing (per-yard subscription + Stripe portal), and notification preferences will live here."
    />
  );
}
