import PhaseStub from "../_components/phase-stub";

export default function AlertsStub() {
  return (
    <PhaseStub
      title="Alerts"
      phase="Coming in Phase 8"
      blurb="Expiration alerts — a text before anything lapses. Set your lead window and recipients; we watch it, you get the heads-up."
    />
  );
}
